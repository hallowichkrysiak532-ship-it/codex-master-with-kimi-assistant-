• The workspace is empty. I'll create both files.

• Now I'll fix small robustness gaps (numpy integer/bool ranks, non-numeric samples), then write the tests.

• Now the constant-triangle check should also cover a constant candidate triangle (pattern_r is undefined either way):

• Now the test file:

• One test is vacuous (a symmetric valid matrix can't have a "scrambled" lower triangle). I'll replace it with a meaningful 4D check.

• Everything is in place. Final handoff:

  # Conclusion

  Implemented the frozen shared-copula transfer primitives module plus focused tests in the assigned (previously empty) workspace. All three public functions match the specified signatures and invariants: eigen-mode selection by descending `abs(eigenvalue - 1)` with stable index tie-breaking, no clipping/jitter, rank 0 → identity, rank d → numerical copy; mean log-density rank selection with exact-tie → smaller rank and full diagnostics; strict-upper-triangle `pattern_r`/`rmse`/`sign_agreement` with d ≥ 3 and constant-triangle rejection.

  # Changed Files

  - `E:\Multi-Agent\workspace\shared_copula_transfer_v1\copula_transfer_primitives.py` (new, ~170 lines)
  - `E:\Multi-Agent\workspace\shared_copula_transfer_v1\test_copula_transfer_primitives.py` (new, ~270 lines, 40+ tests)

  # Key Decisions

  - Matrix validation (module lines 26–42) shared by functions 1 and 3: square, finite, symmetric (rtol 1e-10), unit diagonal (atol 1e-10), strictly PD via Cholesky. Inputs are copied (`astype(float, copy=True)`), never mutated.
  - Bool and `np.bool_` ranks/keys rejected with TypeError; Python and NumPy integers accepted; floats (even 2.0) rejected.
  - `select_rank_from_log_density` enforces equal sample counts across ranks, per the "same evaluation population" clause; unequal lengths raise ValueError.
  - Constant-triangle rejection applies to either reference or candidate, since Pearson r is undefined in both cases (spec stated reference only — symmetric extension, flagged here).
  - Hand-computed anchors in tests: `equicorr(4, 0.5)` rank 1 → off-diagonal exactly 3/11; REF/CAND 3×3 pair → `pattern_r = 1/√13`, `rmse = √(1/3)`, `sign_agreement = 2/3` (both matrices verified PD via leading minors).

  # Tests

  Not run (coder cannot execute). Covers: rank endpoints, intermediate-rank exact value, symmetry/unit-diagonal/PD invariants, tie determinism across calls, nonmutation, all malformed matrix/rank cases, selection optimum + exact tie + diagnostics content, malformed mappings (bool/float/negative keys, unequal lengths, empty/2D/non-finite samples), hand-computed metrics, permutation invariance, shape/d<3/constant-triangle failures.

  # Risks

  - Eigenvectors within exactly degenerate eigenspaces are LAPACK-dependent; determinism is tested via repeated calls, not cross-platform.
  - Tolerance choices (symmetry rtol 1e-10, diagonal atol 1e-10) are interpretive for "close" unit diagonal.

  # Recommended Verification

  Run from `E:\Multi-Agent\workspace\shared_copula_transfer_v1`:
  `python -X utf8 -m pytest -q`
  Review the constant-triangle extension and tolerance choices against experiment needs.

# Workspace Changes

## Added
- `copula_transfer_primitives.py`
- `test_copula_transfer_primitives.py`

## Modified
- None

## Deleted
- None

Full manifest: E:\Multi-Agent\.agent_runtime\results\20260906T062849Z-81eb52fe.changes.json
