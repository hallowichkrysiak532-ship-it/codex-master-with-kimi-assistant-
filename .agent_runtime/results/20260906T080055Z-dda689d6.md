• Now checking CSV counts/alignment and probing the binary .npz key names via Grep.

• Probing decomposition CSV structure (counts per projection, block boundaries, cross-file alignment).

• # Conclusion

  896 行的 Exp28B.1 观测面板与 Exp28C 分解表在 CSV 层面严格对齐（同序 14 域×64，observed_index==sample_index），可按 (observed_index, domain, domain_role) 安全连接；但两个 .npz 银行内部的 source_row_index 对齐与键名在只读、禁执行条件下无法机械核验。128/域的 mode attribution 银行（14×128=1792≠896）不能按 observed_index 位置连接，唯一候选键是 (domain, source_row_index)，其语义未证实。Exp27A 为"子空间稳定但操作化失败即停"，其结论不能当作可用的 nuisance 控制变量来源。

  # Key Findings

  1. 面板结构（全文件逐行核实）：observed_sample_registry.csv 共 896 行，observed_index 0–895；14 域×64：train=Lopex/Angers/IFGG_train/UW_train(256)，historical=UW_val/Bel/Beljap/Bnl/IFGG_val/FFT(384)，external=Cedar/NGEE_arctic/NGEE_panama/QGRAD(256)。source_row_index 在每域块内严格递增、稀疏不连续 → (domain, source_row_index) 唯一，可做空隙连接键。
  2. decomposition_samples.csv 2688 行 = 3 投影(matched/shuffled/global 各 896)×896；每块内 sample_index 0→895 且域序与 registry 完全一致（边界核实：63/64 Lopex→Angers、639/640 FFT→Cedar、895→0 块切换）。该表无 source_row_index，registry 无 E_* 列 → 两表连接键 = observed_index↔sample_index + domain/domain_role。
  3. observed_index 是 Exp28B.1 拼接面板的密集行号；source_row_index 是各域原始源表行号，仅域内唯一。attribution 银行为 128/域：observed_index 在其中不存在，位置连接必然错位；仅当该银行按源表行编号时才可经 (domain, source_row_index) 交集连接，且 896⊂1792 为多对一子集。
  4. 可复用每样本字段：直接观测（registry 列）domain、domain_role、source_row_index、all_primary_traits_interior、boundary_trait_count、mu_clip_l2。派生候选（decomposition 列）E_tangent/E_normal/E_total/eta_tangent/eta_normal/effective_rank/orthogonality——均来自同一白化残差与 Exp20A 切空间投影，作为残差关系模型的"控制"存在循环性；orthogonality 实测 ≤~6e-7，满足 manifest 的 1e-05 门槛，可作 QC 过滤。
  5. Exp27A 边界：x_obs−x_PP(mu) 只是经验未解释残差，非纯传感器噪声；LOSO 子空间仅检验"抗训练源残差的 PP 物理方向"，不确立因果 nuisance 身份或目标不变性。重要性：paired M_NR 分不开 nuisance、不能改进 Exp26B、单光谱（操作化）全部失败 → route_decision=…operational_failed_stop，故不存在冻结的每样本 nuisance 坐标可并入本次连接。

  # Evidence

  （$R=E:\EI\Codes\period 1 prospect_pro and VAE）
  - registry：$R\experiment_v3_20260724\exp28b1…\observed_sample_registry.csv 第 1 行表头；域块边界行 65/129/193/257/321/385/449/513/577/641/705/769/833，末行 897。
  - decomposition：$R\experiment_v3_20260724\exp28c…\decomposition_samples.csv 第 1 行表头、行 896–900、1793–1796、2685–2689；三个 projection 各 896 次（计数核实）。
  - manifest：同目录 protocol_manifest.json 行 4–11（Exp28C 以 exp28b1_state_bank/registry 哈希为父产物）、行 61（orthogonality_max）、行 64–72（target_labels_read/external_labels_read/D_concept_read/G_transfer_read/training 全 false，无标签泄漏设计）。
  - Exp27A：$R\experiment_v3_20260724\exp27a…\exp27a_status.json 行 4、6–17（操作化/单光谱门全 false）、行 49（claim_boundary 原文）；evidence_summary.csv 行 10–13（failed 项中位数 ≤0 或 CI 跨 0）。
  - 三个 .npz（observed_state_bank、pairing_token_bank、$R\output_source_signed_residual_distribution\mode_physical_alignment_v1\sample_attribution_bank.npz）：Grep 仅确认含 ".npy" 成员，为二进制，键名/形状未核验。

  # Uncertainties

  npz 内部数组键名、dtype、行序及两银行 source_row_index 是否逐值一致均未验证（本环境只读且禁执行）；attribution 银行的行语义（128/域对应何样本集）无 manifest 可查；registry 内 source_row_index 跨域重复属正常，但若未来出现域内重复将破坏连接唯一性（当前逐行核实无重复）。

  # Recommended Verification

  运行（只读）：python -X utf8 -c "import numpy as np,pandas as pd; a=np.load(<observed_state_bank.npz>); b=np.load(<pairing_token_bank.npz>); print(a.files,b.files); reg=pd.read_csv(<observed_sample_registry.csv>); [print(k,a[k].shape) for k in a.files]; 比对 a 与 b 中 domain/role/source_row_index 数组与 reg 逐行相等"；再以 c=np.load(sample_attribution_bank.npz) 打印 c.files/shapes，确认其行键是否为 (domain, 源表行号) 后再决定连接方式。

# Workspace Changes

No file changes detected.

Full manifest: E:\Multi-Agent\.agent_runtime\results\20260906T080055Z-dda689d6.changes.json
