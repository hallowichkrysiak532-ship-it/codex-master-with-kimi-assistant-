• The helper scripts are clear. Now I'll write the two new files.

• The pipeline structure is correct. Let me add a manifest clarification about branch-independent reference parameters, then write the test file.

• Now let me clean up unused imports in the test file and do a final review of both files.

• 两个文件已完成并自查。以下为交付摘要（中文，按要求压缩）。

  # Conclusion

  已在工作区内新增且仅新增两个文件，实现历史 Exp20A 有符号残差的"独立参考位置/尺度去除后 source 差异是否保留"敏感性分析，未改动任何既有文件。脚本不可执行验证（未运行任何命令），等待主代理审查后执行。

  # Changed Files

  1. `source_signed_residual_location_scale.py`（约 490 行）
     - L52–72 `quarters_for_ids`：键 `20260905|location_scale_v1|{repeat}|{sample_id}` 的 SHA256 十六进制排序，128→ref_A/eval_A/ref_B/eval_B 各 32；任意唯一且可被 4 整除的计数可用；拒绝重复 ID/非法计数；与输入行序无关。
     - L75–112 `fit_reference`（只接受 ref，签名单参数）/`transform_eval`：raw、center_median、scale_median_iqr（主）、center_mean、scale_mean_sd；缩放分支对 ≤1e-12 的参考尺度直接 `ValueError`，不下限；非有限输入拒绝。
     - L115–153 `correlation_vectors`/`eval_descriptor`/`joint_energy_v`：energy V=2mean(dXY)−mean(dXX)−mean(dYY)，含对角、不开平方，d=Euclidean/√80，cdist/pdist 直接求和。
     - L164–179 `validate_reproducibility_run`：校验 `reproducibility_v1/status.json` 记录的全部产物哈希。
     - `run()` L280 起：现有目录拒绝；protocol_manifest.json 在数值计算前写入（采样键、输入/三个 helper+自身哈希、阶段公式、n_ref=n_eval=32、无 p 值/无 Gaussian 拟合声明、局限）；输出契约全部 9 类产物；相关 RMSE 仅 raw 阶段入表（其余阶段 NaN），非 raw 阶段在前 3 个重复独立重算相关并写入 `affine_correlation_audit.csv`；结束时重验全部输入/产物/代码哈希后写 `status.json`（`complete_historical_residual_location_scale`，不含自身哈希）。`__main__` 用 `threadpool_limits(1)`、Agg 绘图，每 component/stage 打印一次进度。
  2. `test_source_signed_residual_location_scale.py`（约 130 行）：9 个合成测试。

  # Key Decisions

  - 相关不变性：非 raw 阶段复用 raw 的 Pearson/Spearman 向量（数学上逐波段正仿射不变），避免冗余拟合；审计表用前 3 个重复独立重算验证差值≈0，报告与 manifest 明确说明不重复展示、不声称相关"消失"。
  - `reference_parameters.csv.gz` 严格含 contract 列（source, component, repeat, branch, band, start_nm, median, iqr, mean, sd），另加 `ref` 列区分两个独立参考；参数与 branch 无关，按 5 个阶段复制（已在 manifest 说明）。`reference_diagnostics.csv` 每 (source,component,repeat,ref,stat) 记录 min/median/max/count_nonpos。
  - cross = 四个跨 source 分支比较（A/A、A/B、B/A、B/B）均值；within = 两 source 各自 A/B 距离均值；分子分母分别落盘，分母 ≤1e-12 拒绝。
  - 运行成本估计：约 8.4 万次 compare + 同量级 32×80 cdist，纯 Python 循环主导，预计数分钟级。

  # Tests

  覆盖任务要求 6 项：四分组不相交/完备/等大小/行序不变（含不同 repeat 指派不同）；参考拟合+评估变换精确去除已知正仿射（四个分支均有闭式期望值），且换评估块不改变参考参数（含签名单参数检查）；Pearson/Spearman 在正仿射下不变（用 pandas corr 独立实现核对）；joint energy 与直接矩阵公式及 1D `scipy.stats.energy_distance` 平方一致；行置换后边缘 W1=0 而 joint energy>0；退化/近零（5e-13）参考尺度被拒、重复 ID 被拒；另有非有限输入拒绝、描述符相关复用测试。全部使用独立生成数据，不读取真实 bank。

  # Risks

  - 未实际运行：语法/运行时错误（如 pandas 版本对 `set_index`/`loc` 行为差异）需在真机确认。
  - `reference_parameters.csv.gz` 约 190 万行（5 阶段复制），瞬存约几百 MB，若内存紧张可改为单份+manifest 说明。
  - scipy 的 `energy_distance` 与含对角 V 统计量平方的等号依赖 scipy 实现细节（任务已预设该等号）；测试容差 1e-10 相对。
  - `describe()` 要求每波段非退化；若真实评估组某波段恒定会报错（按设计拒绝而非静默）。

  # Recommended Verification

  1. 在含三个 helper 的项目根目录运行：`python -m pytest test_source_signed_residual_location_scale.py -q`。
  2. 合成集成对照（主代理补充）后运行：`python source_signed_residual_location_scale.py --output output_source_signed_residual_distribution/location_scale_v1`（目录须不存在）。
  3. 检查 `affine_correlation_audit.csv` 差值是否≈0、`status.json` 的 `input_hashes_reverified/code_hashes_reverified` 为 true、`report.md` 中 normal 表数值是否合理（例如 scale_median_iqr 后 W1/energy 比值应较 raw 下降但一般仍 >1，具体由数据决定，脚本不做乐观断言）。

# Workspace Changes

## Added
- `source_signed_residual_location_scale.py`
- `test_source_signed_residual_location_scale.py`

## Modified
- None

## Deleted
- None

Full manifest: E:\Multi-Agent\.agent_runtime\results\20260905T154147Z-6c7c7090.changes.json
