• # Conclusion

  Exp28A.2 artifacts can be aligned to the 80 residual 25 nm bands only through a wavelength→physical-window mapping (many 25 nm blocks per 100–300 nm window). They contain no observed-sample IDs or per-observed theta; alignment must use frozen simulator operator tokens, not sample-level matches.

  # Key Findings

  1. Columns/keys: `selected_case_registry.csv`: selected_case_index, case_id, anchor_index, arm, effect, left/right_trait, magnitude, left/right_delta (672 rows). `cross_band_prediction_samples.csv`: case/anchor/shuffled ids, arm, effect, magnitude, target_node, context_node_count, target_signal_fraction, model, relative_error, response_r2, shape/amplitude_discrepancy, delta_rmse, delta_cosine, optimizer_success/nfev (28938 rows). `case_relation_scores.csv`: D_JR_relative/shape/amplitude, predicted_target_count (672). `nonoverlap_context_registry.csv`: target_node, context_node, overlap_wavelength_count, context_allowed (100 = 10×10). NPZ keys (script lines 455–475): truth_response (672×n_wl), truth_token (672×10×4), true_delta (672×6), case_anchor_index, node_scale, token_{main,diag,cross}_{fit,audit} (anchors×10×4×6), median_*_fit, valid_audit_anchor, basis_<node> (Legendre, 1 nm grid; overlap count 100 for blue_pigment confirms 1 nm).
  2. No observed IDs/theta: manifest and status flags `observed_spectra_read=false`, `labels_read=false`; claim_boundary (status line 38) states simulator construct validity only. case_id/anchor_index are simulator-parent (exp28a12) indices; theta = 6-trait bounded delta (±0.02/0.05). shared_transfer_v1 manifest lists observed datasets (Lopex, Angers, …) but no Exp28A.2 artifact references them.
  3. Band mapping (25 nm block k = 400+25k..424+25k): blocks 0–3 blue_pigment; 4–7 cab_green; 8–11 cab_red; 12–15 red_edge; 16–27 nir_structure; 28–35 water_1200; 36–47 water_1450; 54–65 water_1900; 65–77 cbc_swir; protein_2150 nested in blocks 69–70 (inside cbc_swir). Gaps: blocks 48–53 (1600–1749 nm) and 78–79 (2350–2399 nm) uncovered. Overlap: water_1900∩cbc_swir at block 65 (2025–2049 nm); protein nested. Dim 4 tokens per node.
  4. Reference quantity: per-node per-trait operator tokens (`token_main_audit`, `node_scale`, token energy per window) and case-level D_JR/matched-vs-shuffled controls define the simulator relation. Invalid: matching case_id to observed samples; equating matched_M3 errors or delta_cosine with residual-mode scores; treating oracle-M3 accuracy as an observed-data effect; identity-mapping eigenmodes to windows without projection.

  # Evidence

  `exp28a2_state_conditioned_cross_band_relation_v1/` manifest lines 18–132 (windows), 155–165 (flags); status lines 15–25, 38; script `exp28a2_state_conditioned_cross_band_relation.py` lines 382–428, 455–475, 710.

  # Uncertainties

  NPZ keys/shapes inferred from writer code (read-only; could not execute numpy to verify). n_wl presumed 2000 (400–2399 @1 nm) from overlap counts, not confirmed.

  # Recommended Verification

  `python -c "import numpy as np; d=np.load(r'...cross_band_token_bank.npz'); [print(k, v.shape) for k,v in d.items()]"` to confirm keys/shapes and wavelength grid.

# Workspace Changes

No file changes detected.

Full manifest: E:\Multi-Agent\.agent_runtime\results\20260906T071409Z-f406feb9.changes.json
