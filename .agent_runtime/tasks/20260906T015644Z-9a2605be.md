# Delegated Task

- ID: `20260906T015644Z-9a2605be`
- Agent: `coder`
- Workspace: `E:\Multi-Agent\workspace\residual_copula_module_v1`

# Bounded module: empirical copula transforms

Question/deliverable: implement one standalone NumPy/SciPy module for empirical copula transforms plus focused tests. The main agent owns the experiment pipeline, real-data execution and scientific interpretation.

Allowed workspace: the assigned `E:\Multi-Agent\workspace\residual_copula_module_v1` only.

Allowed changes, exactly:
1. `source_empirical_copula.py`
2. `test_source_empirical_copula.py`

Do not read or modify files outside the assigned workspace. Do not create reports, notebooks, experiment pipelines or output directories. You cannot execute tests; report the exact command for the main agent.

Required public functions and invariants:

1. `pseudo_observations(x) -> ndarray`
   - x is a finite 2D sample-by-coordinate array, at least 4 rows and 1 column.
   - Per coordinate use SciPy average ranks: `u=(rankdata(x[:,j], method='average')-0.5)/n`.
   - Return same shape, finite and strictly inside (0,1).
   - For a tie-free coordinate, sorted values equal `(arange(n)+0.5)/n` exactly within floating precision.
   - Ties receive equal midranks. Reject nonfinite or invalid dimensions.
   - Invariant to independently applied strictly increasing transforms of coordinates.

2. `fit_reference_ecdf(reference) -> ndarray`
   - Accept ONLY reference; the single argument prevents evaluation leakage.
   - Same input validation as above.
   - Return values sorted independently by coordinate, shape unchanged. Return a new array.

3. `apply_reference_ecdf(evaluation, sorted_reference) -> ndarray`
   - Evaluation finite 2D, same coordinate count as sorted_reference.
   - Require at least 4 reference rows, sorted nondecreasing in every coordinate and finite.
   - For each evaluation value and coordinate calculate:
     `u=(count(reference < x) + 0.5*count(reference == x) + 0.5)/(n_reference+1)`.
   - Use searchsorted left/right or equivalent; do not loop over reference rows.
   - Return strictly inside (0,1), no clipping, no data-dependent epsilon.
   - Changing evaluation cannot alter sorted_reference.
   - If reference and evaluation are both transformed by the same independently applied strictly increasing coordinate transforms, results must match.

4. `normal_scores(u) -> ndarray`
   - Require finite values strictly inside (0,1); return `scipy.special.ndtri(u)` with same shape and finite values.
   - Reject boundaries, nonfinite values and invalid dimensions.

5. `offdiagonal_dependence(x) -> dict[str, ndarray]`
   - finite 2D with at least 4 rows and at least 2 nondegenerate coordinates.
   - Return the upper-triangle elements of the across-sample Pearson and Spearman correlation matrices as keys `pearson` and `spearman`.
   - Spearman must correctly use average ranks. Reject constant coordinates rather than silently producing NaN.

No randomness, plotting, file I/O or global state. Add type hints and concise docstrings. Avoid classes and unnecessary dependencies.

Focused tests must independently verify:
- exact tie-free pseudo-observation grid and tie midranks;
- invariance under distinct strictly increasing transforms per coordinate;
- reference fit accepts one argument, returns a copy and cannot be changed by evaluating another array;
- hand-computed reference ECDF values below minimum, at an exact tied reference value, between values and above maximum;
- corresponding positive affine-transformed reference/evaluation gives identical reference ECDF output;
- normal-score output finite and matches `scipy.special.ndtri`; invalid 0/1 rejected;
- Pearson/Spearman output matches an independent pandas or direct SciPy calculation and constant coordinates are rejected;
- invalid dimensions/nonfinite/unsorted/mismatched coordinate inputs are rejected.

Acceptance: code must compile and `python -m pytest test_source_empirical_copula.py -q` must pass when the main agent runs it. Preserve all exact formulas above. If an instruction is ambiguous, flag it rather than altering the interface.

Handoff: aim for 1500 characters, maximum 4000. State changed paths, tests not run, and unresolved issues. Deadline: 60 minutes. Use the required coder handoff sections.
