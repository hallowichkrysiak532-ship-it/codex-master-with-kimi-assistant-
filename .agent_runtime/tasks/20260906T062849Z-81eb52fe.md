# Delegated Task

- ID: `20260906T062849Z-81eb52fe`
- Agent: `coder`
- Workspace: `E:\Multi-Agent\workspace\shared_copula_transfer_v1`

# Bounded coder task: frozen shared-copula transfer primitives

## Question or deliverable

Implement a standalone numerical module used by a larger experiment. The main
agent owns dataset I/O, train/validation/test roles, empirical-CDF fitting,
scientific comparisons, artifact generation, and interpretation.

Read `E:\\Multi-Agent\\CONVENTIONS.md` before working.

## Inputs and allowed workspace

Work only in the assigned isolated workspace. Do not read the main project.
Create exactly:

- `copula_transfer_primitives.py`
- `test_copula_transfer_primitives.py`

Use NumPy only. No I/O, plotting, randomness, or mutable global state.

## Public interfaces and invariants

```python
low_rank_correlation(correlation, rank) -> np.ndarray
select_rank_from_log_density(log_density_by_rank) -> tuple[int, dict]
correlation_pattern_metrics(reference, candidate) -> dict
```

1. `low_rank_correlation` accepts a finite symmetric strictly-positive-definite
   correlation matrix with exact/close unit diagonal and integer rank in
   `[0, d]`. Eigen-decompose the matrix and order modes by descending
   `abs(eigenvalue - 1)` with deterministic index tie breaking. Replace omitted
   eigenvalues by one, reconstruct the matrix, symmetrize it, and normalize it
   back to unit diagonal. Rank zero must return identity. Rank `d` must return a
   numerical copy of the input correlation. Reject bool ranks, noninteger ranks,
   invalid matrices, and out-of-range ranks. Never clip eigenvalues or add
   jitter. Do not mutate inputs.
2. `select_rank_from_log_density` accepts a nonempty mapping from distinct
   nonnegative integer ranks to finite, nonempty, one-dimensional arrays of
   per-sample log densities. Arrays may have different lengths only if each
   rank has the same total evaluation population represented; therefore require
   equal lengths. Select the largest arithmetic mean. Exact ties choose the
   smaller rank. Return the selected rank and diagnostics containing sorted
   candidate ranks, corresponding mean scores, sample count, and selected
   score. Reject bool keys and malformed mappings.
3. `correlation_pattern_metrics` validates two same-shape correlation matrices
   and returns upper-triangle `pattern_r`, `rmse`, and `sign_agreement`. Require
   dimension at least three and reject undefined pattern correlation (constant
   upper triangle). `sign_agreement` is the fraction of entries with equal
   `np.sign`.

## Acceptance checks

Focused tests must cover valid endpoints/intermediate rank, symmetry/unit
diagonal/positive definiteness, ordering determinism where observable,
nonmutation, malformed matrix/rank failures, rank-selection optimum and exact
tie behavior, malformed rank-score mappings, exact hand-computable pattern
metrics, and shape/constant-pattern failures.

Do not run commands. Return the coder handoff sections required by the policy,
within 1500 characters. Deadline: 3600 seconds.
