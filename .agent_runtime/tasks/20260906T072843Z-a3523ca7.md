# Delegated Task

- ID: `20260906T072843Z-a3523ca7`
- Agent: `tester`
- Workspace: `E:\Multi-Agent\workspace\mode_physical_alignment_v1`

# Tester task: frozen copula mode attribution primitives

## Question or deliverable

Run the focused pure numerical tests for the frozen copula mode/band
attribution and physical-window mapping. Do not edit files. Read
`E:\\Multi-Agent\\CONVENTIONS.md` first.

## Allowed workspace

`E:\\Multi-Agent\\workspace\\mode_physical_alignment_v1`

The workspace contains a staged copy of the implementation, its focused test,
minimal imported modules, and the frozen Exp28A.2 protocol manifest needed by
one mapping test. Do not access the main project or other paths.

## Commands and acceptance checks

From the workspace root run:

1. `python -X utf8 -m pytest -q -p no:cacheprovider test_source_copula_mode_physical_alignment.py`
2. `python -X utf8 -m py_compile source_copula_mode_physical_alignment.py`

Acceptance: all focused tests pass, compile exits zero, and no files are
modified. Report exact commands, pass count, warnings, and failures using the
tester handoff headings. Handoff at most 1500 characters. Deadline 3600
seconds.
